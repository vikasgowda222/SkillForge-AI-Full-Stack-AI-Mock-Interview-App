# Screenshots

Drop real PNGs here. Filenames referenced from the README:

- `landing.png`     — public landing page (hero + CTA)
- `dashboard.png`   — authenticated dashboard with interview history
- `interview.png`   — in-progress interview (webcam + question + recording controls)
- `feedback.png`    — per-answer feedback with rubric breakdown
- `analytics.png`   — analytics page (timeline + radar + distribution)
- `resume.png`      — resume upload + parsed-text review screen
- `github.png`      — GitHub username input + personalized question preview
- `eval.png`        — `npm run eval` output in the terminal

Recommended dimensions: ~1280×800, optimized PNG (<200 KB each).